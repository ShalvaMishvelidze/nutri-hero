export enum Items {
  center = "items-center",
  start = "items-start",
  end = "items-end",
}

export enum Pb {
  none = "pb-0",
  medium = "pb-[60px]",
}

export enum H {
  small = "h-[360px]",
  regular = "h-[720px] max-xm:h-[480px]",
  max = "h-[calc(100vh-80px)]",
  full = "h-full",
}

export enum ChildrenWidth {
  _1280 = "max-w-[1280px]",
  max = "w-max",
  full = "w-full",
}
