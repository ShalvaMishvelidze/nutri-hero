const shared_style = `size-[33px] text-center text-[22px] leading-[33px] font-bold rounded-full relative z-10 cursor-pointer hover:bg-green hover:text-white`;

export const container = `${shared_style} bg-blue-5 text-black`;

export const container_active = `${shared_style} bg-green text-white`;
