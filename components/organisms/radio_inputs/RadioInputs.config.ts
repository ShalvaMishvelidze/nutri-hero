export enum W {
  full = "w-full",
  auto = "w-auto",
  max = "w-max",
}

export enum RadioW {
  full = "w-full",
  auto = "w-auto",
  max = "w-max",
}

export enum HeadingGap {
  normal = "gap-[28px]",
}

export enum RadioGroupHeight {
  auto = "h-auto",
  _110 = "h-[110px]",
}

export enum RadioInputGap {
  small = "gap-[6px]",
  normal = "gap-[28px]",
}

export enum RadioDirection {
  normal = "flex-row",
  reverse = "flex-row-reverse",
}

export enum RadioJustify {
  between = "justify-between",
  center = "justify-center",
  start = "justify-start",
  end = "justify-end",
}

export enum RadioGap {
  none = "gap-0",
  small = "gap-[6px]",
}
